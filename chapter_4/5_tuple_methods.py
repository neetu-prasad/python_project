#creating a tuple
t = (1, 4, 5, 9, 3, 2)

print(t.count(1)) #counts no. of 1's in tuple

print(t.index(5)) # returns the index of first occurence of 5 in tuple 


#we can say that a tuple is a const list in which we cannot chnge value
